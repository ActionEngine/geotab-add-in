**Introduction**

This exception is thrown on a logon with an incorrect user/password combination or when the user's credentials become invalid.

**Properties**

## InvalidUserType

A value from the InvalidUserType enumeration that indicates the specific reason for the logon failure.